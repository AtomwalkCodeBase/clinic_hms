# Python
__pycache__/
*.py[cod]
*$py.class
venv/
env/
ENV/
*.egg-info/

# Django
*.log
db.sqlite3
db.sqlite3-journal
media/
staticfiles/
static_root/

# Environment / secrets
.env
.env.local
.env.*.local
.env.*backup*
.env.check_backup
EMPLOYEE_LOGIN_CODES.md
INVESTOR_DEMO_CREDENTIALS.md
# Frontend
frontend/node_modules/
frontend/dist/
frontend/dist-verify-tmp*/
frontend/build/
frontend/.env.local
frontend/.env.production
frontend/vite.config.js.timestamp-*.mjs

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Misc
*.bak
*.tmp

# Runtime output — logs (celery-*.log, doc-pipeline.log), Celery/Flower state
/logs/
celerybeat-schedule*
celerybeat.pid
*.pid
flower.db*

# Internal planning docs (not meant for the repo)
/Atomwalk_HMS_Knowledge_Map.docx
/Atomwalk_HMS_Knowledge_Map.md
/Clinical_HMS_External_System_Comparison.docx
/HMS_OPD_IPD_HowItConnects_updated_v2.pptx
/HMS_OPD_IPD_HowItConnects_updated.pptx
/IPD_Admission_Feature_Documentation_v2.docx
/IPD_Admission_Feature_Documentation.docx
/IPD_Full_Cycle.pptx

# Internal planning folders (not meant for the repo) — previously listed
# below as absolute Windows paths, which gitignore never actually matched
# (it needs repo-relative patterns), so these were silently untracked-but-
# not-ignored until now.
/Claude outputs/
/docs/
/.claude/
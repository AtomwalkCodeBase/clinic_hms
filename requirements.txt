Django==4.2.13
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1
psycopg2-binary==2.9.9
django-environ==0.11.2
django-cors-headers==4.3.1
Pillow==10.3.0
python-decouple==3.8
drf-spectacular==0.27.2
django-filter==24.2
gunicorn==22.0.0
whitenoise==6.7.0
PyJWT==2.8.0
python-json-logger==2.0.7
sentry-sdk==2.13.0
cryptography==43.0.0
reportlab==4.2.5
qrcode==8.2
phonenumbers==8.13.45
dnspython==2.6.1
requests==2.32.3
boto3==1.34.144
rapidfuzz==3.9.7
pypdf==6.15.0
# ── OCR for no-QR document classification (core/ocr.py) ──
rapidocr-onnxruntime==1.3.24  # primary engine: PaddleOCR's PP-OCR models on ONNX Runtime — pip-only, CPU, no system package (pulls opencv-python-headless, numpy, onnxruntime)
pymupdf==1.24.10              # rasterise image-only / scanned PDFs so they can be OCR'd
# ── Background jobs: My Reports pipeline (core.celery_runtime) + mobile upload-and-extract (apps/registry/tasks.py) ──
celery==5.3.6
django-celery-beat==2.7.0
django-celery-results==2.5.1
SQLAlchemy==2.0.36            # kombu's Postgres broker transport (local dev, no Redis needed)
redis==5.0.8                  # Redis broker client: production sets CELERY_BROKER_URL=redis://127.0.0.1:6379/0
psutil==6.1.0                 # start/stop/monitor worker & beat processes
flower==2.0.1                 # Celery monitoring dashboard (needs a Redis/RabbitMQ broker)
